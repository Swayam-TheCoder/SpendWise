Note: if you changed the schema.prisma then make sure to run
npx prisma migrate dev --name init
npx prisma generate 


TOPIC: verification token generation + email service, Verification of email endpoints, Resend Verification Email

  -Generated a secure random token.
  1. Add token helper
  We use crypto.randomBytes() rather than Math.random() because this token is security-sensitive.

  -Creating email service from nodemailer
  EMAIL_USER=yourEmail@gmail.com
  EMAIL_PASSWORD=gmailappPassword

  EMAIL_PASSWORD should not be your normal Gmail password.
  Use a Google App Password.


So the signup flow becomes
User Signup
    ↓
Validate
    ↓
Hash Password
    ↓
Create User
    ↓
Generate random token
    ↓
Hash token
    ↓
Store hash in PostgreSQL
    ↓
Send original token through email


  -Verify Email

    Flow:
    POST /api/auth/verify-email
            ↓
    Validate token
            ↓
    Hash token
            ↓
    Find token
            ↓
    Check expiration
            ↓
    Verify user
            ↓
    Delete token
            ↓
    isEmailVerified = true


Get the actual token from the verification email.

then: POST /api/auth/verify-email
Body:
{
  "token": "TOKEN_FROM_EMAIL" // its a verification email not the hashToken
}

Expected:
{
  "success": true,
  "message": "Email verified successfully"
}

Prisma studio should show now {isEmailVerified: true}

Security behavior:
Valid token
    ↓
Verify email
    ↓
Delete token
    ↓
Cannot use same token again

Expired token:
Expired token
    ↓
Delete token
    ↓
Verification fails

Invalid token:
Fake token
    ↓
No matching hash
    ↓
400 Bad Request


    -Resend Verification Email
    Flow:
    POST /api/auth/resend-verification
            ↓
    Find user
            ↓
    Already verified?
            ↓
    Generate new token
            ↓
    Delete old tokens
            ↓
    Store new token hash
            ↓
    Send email


Authentication is now
Signup
  ↓
Argon2 password
  ↓
Email verification
  ↓
Login
  ↓
Access + Refresh tokens
  ↓
Protected APIs
  ↓
Refresh token rotation
  ↓
Logout